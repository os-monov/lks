export class BatchWriterService {
  // bucket
  // lock
  // lastCommitTimestamp: Date
  // buffer: Message[]
  // buffer
  // write(message, callback) {
  // if bufferFull() {
  //      flush()
  //}
  // }
  // bufferFull()
  // make size and duration configurable
  // either 4mb of buffer or now - lastCommitTimestamp >= 150ms
  // flush()
  // file = File()
  // bucket.putObject(file)
  // commit(topic, partition, offset, file)
  // callback()
}
